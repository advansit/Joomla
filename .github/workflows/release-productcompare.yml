name: Release - J2Commerce Product Compare

on:
  push:
    tags:
      - 'productcompare-v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.0.1)'
        required: true
        type: string

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Extract version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/productcompare-v}
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Build extension
        run: |
          cd J2Commerce/plg_j2commerce_productcompare
          chmod +x build.sh
          ./build.sh

      - name: Find package file
        id: package
        run: |
          cd J2Commerce/plg_j2commerce_productcompare
          PACKAGE=$(ls -1 plg_j2commerce_productcompare_*.zip | head -1)
          echo "file=$PACKAGE" >> $GITHUB_OUTPUT
          echo "Package: $PACKAGE"

      - name: Generate changelog
        id: changelog
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 productcompare-v${{ steps.version.outputs.version }}^ 2>/dev/null | grep "^productcompare-" || echo "")
          if [ -z "$PREV_TAG" ]; then
            echo "changelog=Initial release" >> $GITHUB_OUTPUT
          else
            CHANGELOG=$(git log ${PREV_TAG}..productcompare-v${{ steps.version.outputs.version }} --pretty=format:"- %s" --no-merges -- J2Commerce/plg_j2commerce_productcompare/)
            if [ -z "$CHANGELOG" ]; then
              CHANGELOG="- Bug fixes and improvements"
            fi
            echo "changelog<<EOF" >> $GITHUB_OUTPUT
            echo "$CHANGELOG" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: Create or update tag
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a productcompare-v${{ steps.version.outputs.version }} -m "Release v${{ steps.version.outputs.version }}"
          git push origin productcompare-v${{ steps.version.outputs.version }}

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: productcompare-v${{ steps.version.outputs.version }}
          name: J2Commerce Product Compare v${{ steps.version.outputs.version }}
          body: |
            ## J2Commerce Product Compare Plugin v${{ steps.version.outputs.version }}
            
            Product comparison functionality for J2Commerce stores.
            
            ### Changes
            ${{ steps.changelog.outputs.changelog }}
            
            ### Installation
            1. Download `${{ steps.package.outputs.file }}`
            2. Install via Joomla Extension Manager
            3. Enable plugin in System → Plugins
            
            ### Requirements
            - Joomla 4.x, 5.x or 6.x
            - PHP 8.0 or higher
            - J2Commerce 3.x or higher
          files: |
            J2Commerce/plg_j2commerce_productcompare/${{ steps.package.outputs.file }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

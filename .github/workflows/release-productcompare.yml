name: Release - J2Commerce Product Compare

on:
  push:
    tags:
      - 'productcompare-v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.0.1)'
        required: true
        type: string

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/productcompare-v}
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Update version in files
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          cd j2commerce/plg_j2commerce_productcompare
          echo "$VERSION" > VERSION
          sed -i "s/VERSION=\".*\"/VERSION=\"$VERSION\"/" build.env
          sed -i "s/<version>.*<\/version>/<version>$VERSION<\/version>/" plg_j2commerce_productcompare.xml

      - name: Build extension
        run: |
          cd j2commerce/plg_j2commerce_productcompare
          chmod +x build.sh
          ./build.sh

      - name: Find package file
        id: package
        run: |
          cd j2commerce/plg_j2commerce_productcompare
          PACKAGE=$(ls -1 plg_j2commerce_productcompare_*.zip | head -1)
          echo "file=$PACKAGE" >> $GITHUB_OUTPUT
          echo "Package: $PACKAGE"

      - name: Update update.xml
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          PACKAGE="${{ steps.package.outputs.file }}"
          cat > j2commerce/plg_j2commerce_productcompare/updates/update.xml << EOF
          <?xml version="1.0" encoding="utf-8"?>
          <updates>
              <update>
                  <name>J2Commerce Product Compare</name>
                  <description>Product comparison functionality for J2Commerce</description>
                  <element>productcompare</element>
                  <type>plugin</type>
                  <folder>j2store</folder>
                  <version>$VERSION</version>
                  <infourl>https://github.com/advansit/Joomla/tree/main/j2commerce/plg_j2commerce_productcompare</infourl>
                  <downloads>
                      <downloadurl type="full" format="zip">https://github.com/advansit/Joomla/releases/download/productcompare-v$VERSION/$PACKAGE</downloadurl>
                  </downloads>
                  <tags>
                      <tag>stable</tag>
                  </tags>
                  <targetplatform name="joomla" version="(4\.[0-9]|5\.[0-9]|6\.[0-9])" />
                  <php_minimum>8.1</php_minimum>
              </update>
          </updates>
          EOF

      - name: Commit version updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add j2commerce/plg_j2commerce_productcompare/VERSION
          git add j2commerce/plg_j2commerce_productcompare/build.env
          git add j2commerce/plg_j2commerce_productcompare/plg_j2commerce_productcompare.xml
          git add j2commerce/plg_j2commerce_productcompare/updates/update.xml
          git diff --staged --quiet || git commit -m "Release productcompare v${{ steps.version.outputs.version }} [skip ci]"
          git push origin HEAD:main || true

      - name: Generate changelog
        id: changelog
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null | grep "^productcompare-" || echo "")
          if [ -z "$PREV_TAG" ]; then
            echo "changelog=Initial release" >> $GITHUB_OUTPUT
          else
            CHANGELOG=$(git log ${PREV_TAG}..HEAD --pretty=format:"- %s" --no-merges -- j2commerce/plg_j2commerce_productcompare/ | head -20)
            if [ -z "$CHANGELOG" ]; then
              CHANGELOG="- Bug fixes and improvements"
            fi
            echo "changelog<<EOF" >> $GITHUB_OUTPUT
            echo "$CHANGELOG" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: Create or update tag
        if: github.event_name == 'workflow_dispatch'
        run: |
          git tag -fa productcompare-v${{ steps.version.outputs.version }} -m "Release v${{ steps.version.outputs.version }}"
          git push origin productcompare-v${{ steps.version.outputs.version }} --force

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: productcompare-v${{ steps.version.outputs.version }}
          name: J2Commerce Product Compare v${{ steps.version.outputs.version }}
          body: |
            ## J2Commerce Product Compare Plugin v${{ steps.version.outputs.version }}
            
            Product comparison functionality for J2Commerce stores.
            
            ### Changes
            ${{ steps.changelog.outputs.changelog }}
            
            ### Installation
            1. Download `${{ steps.package.outputs.file }}`
            2. Install via Joomla Extension Manager
            3. Enable plugin in System → Plugins
            
            ### Requirements
            - Joomla 4.x, 5.x or 6.x
            - PHP 8.1 or higher
            - J2Commerce 4.x or higher
          files: |
            j2commerce/plg_j2commerce_productcompare/${{ steps.package.outputs.file }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
